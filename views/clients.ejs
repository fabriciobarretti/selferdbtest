<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <!-- <link rel="stylesheet" href="/styles/style.css"> -->
  <title>MySQL Table in Node.js</title>
</head>
<body>
  <h1>MySQL Table Data</h1>
  <table>
    <thead>
      <tr>
        <% dataKeys.forEach( (rowName) => { %>
            <th><%= rowName %></th>
        <% });%>
        <!-- <th>ID</th>
        <th>Name</th>
        <th>Age</th>
        Add more columns based on your table structure -->
      </tr>
    </thead>
    <tbody>
      <% data.forEach((row) => { %>
        <tr>
          <td><%= row.id %></td>
          <td><%= row.name %></td>
          <td><%= row.age %></td>
          <!-- Add more columns based on your table structure -->
        </tr>
      <% }); %>
    </tbody>
  </table>
</body>
</html>